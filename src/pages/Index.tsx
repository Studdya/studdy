
// This file is needed for compatibility but we're using TimerPage as our index
// All routing is handled in App.tsx
import TimerPage from "./TimerPage";

const Index = () => {
  return <TimerPage />;
};

export default Index;
